import 'package:flutter/cupertino.dart';

class PatientBox extends StatelessWidget {
  const PatientBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
